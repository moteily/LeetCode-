{
    "tasks": [
        {
            "type": "shell",
            "label": "clang-3.8 build active file",
            "command": "/usr/bin/clang-3.8",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}